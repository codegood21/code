nama=Html
language=html
lines=259
date=26/10/2025
code=<!DOCTYPE html>
<html lang="id">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>F4X0L HTML GENERATOR</title>
<style>
    @import url('https://fonts.googleapis.com/css2?family=Share+Tech+Mono&display=swap');
    * { margin: 0; padding: 0; box-sizing: border-box; }
    body {
        font-family: 'Share Tech Mono', monospace;
        background: #000;
        color: #00ff00;
        min-height: 100vh;
        overflow-x: hidden;
        position: relative;
    }

    /* MATRIX RAIN BACKGROUND */
    canvas#matrixCanvas {
        position: fixed;
        top: 0; left: 0;
        width: 100%; height: 100%;
        z-index: -1;
        background: #000;
    }

    /* Container */
    .container {
        max-width: 900px;
        margin: 50px auto;
        padding: 20px;
        position: relative;
        z-index: 1;
    }

    /* Glitch title */
    h1 {
        font-size: 3em;
        text-align: center;
        color: #00ff00;
        position: relative;
        animation: glitch 1s infinite;
        margin-bottom: 30px;
    }

    @keyframes glitch {
        0% { text-shadow: 2px 0 #ff00ff, -2px 0 #00ffff; }
        25% { text-shadow: -2px 0 #ff00ff, 2px 0 #00ffff; }
        50% { text-shadow: 2px 0 #ff00ff, -2px 0 #00ffff; }
        75% { text-shadow: -2px 0 #ff00ff, 2px 0 #00ffff; }
        100% { text-shadow: 2px 0 #ff00ff, -2px 0 #00ffff; }
    }

    /* Input Section */
    .input-section {
        background: rgba(0,0,0,0.8);
        border: 2px solid #00ff00;
        border-radius: 12px;
        padding: 25px;
        margin-bottom: 30px;
        box-shadow: 0 0 20px #00ff00;
    }

    .input-group { display: flex; gap: 15px; margin-bottom: 20px; }
    input[type="url"] {
        flex: 1;
        padding: 15px;
        background: rgba(0,0,0,0.6);
        border: 2px solid #00ff00;
        border-radius: 8px;
        color: #00ff00;
        font-size: 16px;
        transition: all 0.3s;
    }
    input[type="url"]:focus {
        outline: none;
        box-shadow: 0 0 10px #00ff00;
    }

    button {
        padding: 15px 30px;
        background: linear-gradient(45deg, #00ff00, #00aa00);
        border: 2px solid #00ff00;
        border-radius: 8px;
        font-weight: bold;
        cursor: pointer;
        transition: all 0.3s;
        position: relative;
        color: #000;
    }
    button:hover {
        transform: translateY(-2px);
        box-shadow: 0 0 15px #00ff00, 0 0 20px #00aa00;
    }

    /* Loading */
    .loading { display: none; text-align: center; margin: 20px 0; }
    .loading.active { display: block; }
    .loader {
        display: inline-block;
        width: 40px; height: 40px;
        border: 3px solid rgba(0,255,0,0.3);
        border-radius: 50%;
        border-top-color: #00ff00;
        animation: spin 1s linear infinite;
    }

    @keyframes spin { to { transform: rotate(360deg); } }

    /* Result Section */
    .result-section {
        background: rgba(0,0,0,0.85);
        border: 2px solid #00ff00;
        border-radius: 12px;
        padding: 20px;
        display: none;
        position: relative;
        overflow: hidden;
        box-shadow: 0 0 20px #00ff00;
    }
    .result-section.active { display: block; animation: fadeIn 0.5s ease; }
    @keyframes fadeIn { from { opacity: 0; transform: translateY(20px); } to { opacity: 1; transform: translateY(0); } }

    .result-meta { display: flex; gap: 20px; margin-bottom: 15px; font-size: 14px; }
    .result-meta span { padding: 5px 10px; background: rgba(0,255,0,0.1); border-radius: 5px; }

    .result-content {
        background: rgba(0,0,0,0.6);
        border-radius: 8px;
        padding: 15px;
        max-height: 400px;
        overflow-y: auto;
        position: relative;
    }

    .code-output {
        font-family: 'Courier New', monospace;
        font-size: 14px;
        color: #00ff00;
        white-space: pre-wrap;
        word-break: break-all;
    }

    .copy-button {
        position: absolute;
        top: 10px; right: 10px;
        padding: 6px 12px;
        background: rgba(0,255,0,0.2);
        border: 1px solid #00ff00;
        border-radius: 5px;
        cursor: pointer;
        transition: all 0.3s;
    }
    .copy-button:hover { background: rgba(0,255,0,0.3); }
    .copy-button.copied { background: rgba(0,255,0,0.4); border-color: #00ff00; }

    @media (max-width: 768px) {
        h1 { font-size: 2em; }
        .input-group { flex-direction: column; }
        .result-meta { flex-direction: column; gap: 10px; }
    }
</style>
</head>
<body>
<canvas id="matrixCanvas"></canvas>
<div class="container">
    <h1>F4X0L HTML GENERATOR</h1>
    <div class="input-section">
        <div class="input-group">
            <input type="url" id="urlInput" placeholder="Masukkan URL (wajib https://)">
            <button onclick="generateCode()">Generate Code</button>
        </div>
        <div class="loading" id="loading">
            <div class="loader"></div>
            <p style="margin-top:10px;color:#00ff00;">Mengambil kode dari server cyber...</p>
        </div>
    </div>
    <div class="result-section" id="resultSection">
        <div class="result-meta">
            <span id="creator">Creator: -</span>
            <span id="status">Status: -</span>
        </div>
        <div class="result-content">
            <button class="copy-button" onclick="copyCode()">Salin Kode</button>
            <pre class="code-output" id="codeOutput"></pre>
        </div>
    </div>
</div>

<script>
// MATRIX RAIN
const canvas = document.getElementById("matrixCanvas");
const ctx = canvas.getContext("2d");
canvas.width = window.innerWidth;
canvas.height = window.innerHeight;

const letters = "ABCDEFGHIJKLMNOPQRSTUVWXYZ123456789@#$%^&*()*&^%";
const fontSize = 14;
const columns = canvas.width / fontSize;
const drops = [];
for (let x=0; x<columns; x++) drops[x]=1;

function drawMatrix(){
    ctx.fillStyle="rgba(0,0,0,0.05)";
    ctx.fillRect(0,0,canvas.width,canvas.height);
    ctx.fillStyle="#00ff00";
    ctx.font=fontSize+"px monospace";
    for(let i=0;i<drops.length;i++){
        const text=letters.charAt(Math.floor(Math.random()*letters.length));
        ctx.fillText(text,i*fontSize,drops[i]*fontSize);
        if(drops[i]*fontSize>canvas.height && Math.random()>0.975) drops[i]=0;
        drops[i]++;
    }
}
setInterval(drawMatrix,50);
window.addEventListener("resize",()=>{canvas.width=window.innerWidth; canvas.height=window.innerHeight;});

// Generate Code
async function generateCode() {
    const url = document.getElementById('urlInput').value.trim();
    if(!url){ alert('Masukkan URL terlebih dahulu!'); return; }
    if(!url.startsWith('https://')){ alert('URL wajib https://'); return; }

    const loading = document.getElementById('loading');
    const resultSection = document.getElementById('resultSection');
    loading.classList.add('active');
    resultSection.classList.remove('active');

    try {
        const response = await fetch(`https://api.resellergaming.my.id/tools/getcode?url=${encodeURIComponent(url)}`);
        const data = await response.json();
        if(data.status){
            document.getElementById('creator').textContent = `Creator: ${data.creator}`;
            document.getElementById('status').textContent = `Status: ${data.status?'Success':'Failed'}`;
            document.getElementById('codeOutput').textContent = data.result.html;
            loading.classList.remove('active');
            resultSection.classList.add('active');
        } else throw new Error('Fetch gagal');
    } catch(err){
        loading.classList.remove('active');
        alert('Terjadi kesalahan saat mengambil kode.');
        console.error(err);
    }
}

// Copy code
function copyCode(){
    const codeOutput=document.getElementById('codeOutput');
    const copyButton=document.querySelector('.copy-button');
    navigator.clipboard.writeText(codeOutput.textContent).then(()=>{
        copyButton.textContent='Tersalin!';
        copyButton.classList.add('copied');
        setTimeout(()=>{copyButton.textContent='Salin Kode'; copyButton.classList.remove('copied');},2000);
    }).catch(err=>{alert('Gagal menyalin kode');console.error(err);});
}
</script>
</body>
</html>