case 'sertifikat': {
 if (!text) return m.reply('Contoh: .sertifikat Yanto Kopling');

 try {
 const axios = require('axios');
 const url = `https://api.siputzx.my.id/api/m/sertifikat-tolol?text=${encodeURIComponent(text)}`;

 const res = await axios.get(url, { responseType: 'arraybuffer' });
 const buffer = Buffer.from(res.data, 'binary');

 await conn.sendMessage(m.chat, { image: buffer, caption: `✅ Doneee Bosss Quuu !!` }, { quoted: m });
 } catch (err) {
 console.error(err);
 m.reply('❌ Terjadi kesalahan saat membuat Sertifikat.');
 }
}
break