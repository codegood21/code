nama=pluginFeature
language=javascript
lines=1
date=17/01/2026
code=import fs from "fs" let handler = async (m, { ky, reply, text, args, isOwner, isGroup, quoted, command }) => { if (!isOwner) return reply("Fitur ini khusus owner.") const basePluginPath = "./esm/" const baseFilePath = "./esm" const fsp = fs.promises if (!fs.existsSync(basePluginPath)) { fs.mkdirSync(basePluginPath, { recursive: true }) } if (/addplugin/i.test(command)) { if (!text) return reply("Format: /addplugin namafile.mjs (reply dengan kode atau sertakan kode setelah nama)") const name = args[0] if (!name) return reply("Nama file plugin wajib diisi, contoh: /addplugin namafile.mjs") const filename = name.endsWith(".mjs") ? name : name + ".mjs" let content = "" if (quoted && quoted.isText() && quoted.getText()) { content = quoted.getText() } else { const parts = text.split(" ").slice(1) if (!parts.length) return reply("Reply teks kode plugin atau sertakan kode setelah nama file.") content = parts.join(" ") } const filePath = `${basePluginPath}/${filename}` await fsp.writeFile(filePath, content, "utf-8") return reply(`Plugin berhasil disimpan sebagai ${filename}`) } if (/listplugin/i.test(command)) { const files = await fsp.readdir(basePluginPath) if (!files.length) return reply("Belum ada plugin tersimpan.") const list = files.map((f, i) => `${i + 1}. ${f}`).join("\n") return reply("Daftar plugin:\n" + list) } if (/delplugin/i.test(command)) { if (!args[0]) return reply("Format: /delplugin namafile.mjs") const filename = args[0].endsWith(".mjs") ? args[0] : args[0] + ".mjs" const filePath = `${basePluginPath}/${filename}` if (!fs.existsSync(filePath)) return reply("Plugin tidak ditemukan.") await fsp.unlink(filePath) return reply(`Plugin ${filename} berhasil dihapus.`) } if (/geplugin/i.test(command)) { if (!args[0]) return reply("Format: /geplugin namafile.mjs") const filename = args[0].endsWith(".mjs") ? args[0] : args[0] + ".mjs" const filePath = `${basePluginPath}/${filename}` if (!fs.existsSync(filePath)) return reply("Plugin tidak ditemukan.") const content = await fsp.readFile(filePath, "utf-8") return reply(`Isi plugin ${filename}:\n\n${content}`) } if (/gantifile/i.test(command)) { if (args.length < 2) return reply("Format: /gantifile namalama.mjs namabaru.mjs") const oldName = args[0].endsWith(".mjs") ? args[0] : args[0] + ".mjs" const newName = args[1].endsWith(".mjs") ? args[1] : args[1] + ".mjs" const oldPath = `${basePluginPath}/${oldName}` const newPath = `${basePluginPath}/${newName}` if (!fs.existsSync(oldPath)) return reply("File lama tidak ditemukan.") await fsp.rename(oldPath, newPath) return reply(`File ${oldName} berhasil diganti menjadi ${newName}.`) } if (/getfile/i.test(command)) { if (!args[0]) return reply("Format: /getfile namafile.mjs") const filename = args[0] const filePath = `${baseFilePath}/${filename}` if (!fs.existsSync(filePath)) return reply("File tidak ditemukan.") const content = await fsp.readFile(filePath, "utf-8") return reply(`Isi file ${filename}:\n\n${content}`) } if (/listfile/i.test(command)) { const files = await fsp.readdir(baseFilePath) if (!files.length) return reply("Tidak ada file di folder esm.") const list = files.map((f, i) => `${i + 1}. ${f}`).join("\n") return reply("Daftar file di ./esm:\n" + list) } } handler.help = ["addplugin", "listplugin", "delplugin", "geplugin", "gantifile", "getfile", "listfile"] handler.tags = ["OWNER MENU"] handler.command = ["addplugin", "listplugin", "delplugin", "geplugin", "gantifile", "getfile", "listfile"] export default handler